export enum Feature {
    Login = "login",
    Logout = "logout"
}

export enum TestType {
    Smoke = "smoke",
    Regression = "regression"
}

export enum Environment {
    Development = "dev",
    Production = "prod",
    Staging = "stage" 
}